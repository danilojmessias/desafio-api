{
  "name": "user-api-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "test:report": "npm test && npm run allure:serve",
    "allure:generate": "npx allure generate allure-results -o allure-report --clean",
    "allure:serve": "npx allure serve allure-results",
    "allure:open": "npx allure open allure-report"
  },
  "dependencies": {
    "@faker-js/faker": "^9.9.0",
    "supertest": "^6.3.3"
  },
  "devDependencies": {
    "allure-commandline": "^2.34.1",
    "allure-js-commons": "^3.3.2",
    "jest": "^29.6.1",
    "jest-allure": "^0.1.3",
    "jest-allure2-reporter": "^1.0.4"
  }
}
